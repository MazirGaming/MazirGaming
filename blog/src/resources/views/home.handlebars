<div class="mt-4">
  <div class="home-filter">
    <span class="home-filter__label">Sắp xếp theo</span>
    {{!-- <a class="home-filter-btn btn">Phổ Biến</a> --}}
    <a href="/?sort=-createdAt" class="home-filter-btn btn btn--primary createAt">Mới Nhất</a>
    {{!-- <button class="home-filter-btn btn">Bán Chạy</button> --}}
    <div class="select-input">
      <span class="select-input__label">Giá</span>
      <i class="select-input__icon fas fa-chevron-down"></i>
      <ul class="select-input__list">
        <li class="select-input__item">
          <a href="/?sort=price" class="select-input__link">Giá: Thấp đến Cao</a>
        </li>
        <li class="select-input__item">
          <a href="/?sort=-price" class="select-input__link">Giá: Cao đến Thấp</a>
        </li>
      </ul>
    </div>
  </div>
  <div class="row mt-4">
    {{#each gifts}}
    <div class="col-3">
      <div class="card mt-4">
          <div class="product-name">

        <a class="gift-slug" href="/gifts/{{this.slug}}">
          <img class="card-img-top" src="{{this.image}}" alt="Card image cap">

          <h5 class="gift-name">{{this.name}}</h5>
          {{!-- <h5>giảm giá {{this.discount}}%</h5>
          <h5>Giá sau giảm: 
            {{discount this.price this.discount}}
          </h5>
            {{#if (zero this.qty)}}
            <h5>Hết hàng</h5>
            {{else}}
            <h5>Còn {{this.qty}} sản phẩm</h5>
            {{/if}} --}}
        </a>
          </div>

        <h4 class="gift-price">{{this.price}} vnđ</h4>
        {{!-- <p>{{this._id}}</p> --}}
        <a class="cart-add" href="/cart/add/{{this._id}}">Thêm vào giỏ hàng</a>
        <form action="/cart/add/{{this.slug}}" method="post">
          <input type="hidden" name="_id" value="{{this._id}}">
          {{!-- <button type="submit" class="btn btn-primary add-cart">Add to Cart</button> --}}
        </form>
      </div>
    </div>
    {{/each}}
  </div>
  <div class="mt-4"></div>
  <nav aria-label="Page navigation example">
    <ul class="pagination">
      
    </ul>
  </nav>
</div>
{{#if mess}}
<div class="alert alert-warning" role="alert">
  <p>{{mess}}</p>
</div>
{{/if}}
{{!-- <a class="button">1</a>
<a class="button">2</a>
<a class="button">3</a> --}}
<script>
  var ulElement = document.querySelector('.pagination')
  var liElements = document.querySelectorAll('.page-item')
  var html = ''

  for (var i = 1; i <= {{ totalPage }}; i++) {
    ulElement.innerHTML = '<li class="page-item"><a class="page-link" href="?page=' + i + '">' + i + '</a></li>';
    html += ulElement.innerHTML
  }
  ulElement.innerHTML = html

      var aElements = document.querySelectorAll('.page-link')

      $('.button').on('click', function(){
        $('.button').removeClass('active');
        $(this).addClass('active')
      })
      
    

   
    
{{!-- var a = 100000
var a = new Intl.NumberFormat('vi-VN', {
      style:'currency',
      currency: 'VND',
    }).format(a)
console.log(a) --}}
</script>